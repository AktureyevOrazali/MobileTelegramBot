Код формы внешней обработки

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
    СписокРазделов = Новый СписокЗначений;
    Для Каждого Стр Из Объект.ДоступныеРазделыMobileBot() Цикл
        СписокРазделов.Добавить(Стр.Наименование, Стр.Код);
    КонецЦикла;

    ИсторияСообщений = Новый ТаблицаЗначений;
    ИсторияСообщений.Колонки.Добавить("MessageId");
    ИсторияСообщений.Колонки.Добавить("Дата");
    ИсторияСообщений.Колонки.Добавить("Направление");
    ИсторияСообщений.Колонки.Добавить("Автор");
    ИсторияСообщений.Колонки.Добавить("Текст");
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
    Если ЗначениеЗаполнено(ExternalChatId) Тогда
        ОбновитьИзРегистра();
        ЗаполнитьИсториюИзРегистра();
        ОбновитьИсторию();
    КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ExternalChatIdПриИзменении(Элемент)
    ОбновитьИзРегистра();
    ЗаполнитьИсториюИзРегистра();
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьИзРегистра()
    Если ПустаяСтрока(ExternalChatId) Тогда Возврат; КонецЕсли;
    Данные = ПолучитьСоответствиеНаСервере(ExternalChatId);
    Если Данные <> Неопределено Тогда
        ChatId   = Данные.ChatId;
        DialogId = Данные.DialogId;
    КонецЕсли;
КонецПроцедуры

&НаСервере
Функция ПолучитьСоответствиеНаСервере(ExternalChatId)
    Возврат РеквизитФормыВЗначение("Объект").ПолучитьСоответствиеMobileBot(ExternalChatId);
КонецФункции

&НаКлиенте
Процедура ЗаполнитьИсториюИзРегистра()
    Если ПустаяСтрока(ExternalChatId) Тогда Возврат; КонецЕсли;
    Сообщения = ВыгрузитьИсториюНаСервере(ExternalChatId);
    ЗаполнитьТаблицуИстории(Сообщения);
КонецПроцедуры

&НаСервере
Функция ВыгрузитьИсториюНаСервере(ExternalChatId)
    Возврат РеквизитФормыВЗначение("Объект").ВыгрузитьИсториюMobileBot(ExternalChatId);
КонецФункции

&НаКлиенте
Процедура ЗаполнитьТаблицуИстории(Сообщения)
    ИсторияСообщений.Очистить();
    Если Сообщения = Неопределено Тогда
        ИсторияТекст = "";
        Возврат;
    КонецЕсли;
    Для Каждого Сообщение Из Сообщения Цикл
        Стр = ИсторияСообщений.Добавить();
        Стр.MessageId   = Сообщение.message_id;
        Стр.Дата        = ПолучитьДатуИзISOНаСервере(Сообщение.created_at);
        Стр.Направление = ?(Сообщение.direction = "incoming", "Клиент", "Оператор");
        Стр.Автор       = Сообщение.author;
        Стр.Текст       = Сообщение.text;
    КонецЦикла;
    ИсторияТекст = ?(ИсторияСообщений.Количество() > 0,
                     ИсторияСообщений[ИсторияСообщений.Количество()-1].Текст, "");
КонецПроцедуры

&НаСервере
Функция ПолучитьДатуИзISOНаСервере(Знач ДатаСтрока)
    Возврат Объект.ДатаИзISO8601(ДатаСтрока);
КонецФункции

&НаКлиенте
Процедура КомандаОтправить(Команда)
    Попытка
        ПараметрыОтправки = Новый Структура;
        ПараметрыОтправки.Вставить("ExternalChatId", ExternalChatId);
        ПараметрыОтправки.Вставить("MessageText", MessageText);
        ПараметрыОтправки.Вставить("BIN", BIN);
        ПараметрыОтправки.Вставить("Author", Author);
        ПараметрыОтправки.Вставить("Title", Title);
        ПараметрыОтправки.Вставить("Section", Section);
        ПараметрыОтправки.Вставить("ChatId", ChatId);
        
        Результат = ОтправитьСообщениеНаСервере(ПараметрыОтправки);
        
        Если Результат <> Неопределено Тогда
            ChatId   = Результат.chat_id;
            DialogId = Результат.dialog_id;
            СохранитьСоответствиеНаСервере(ExternalChatId, ChatId, DialogId);
            MessageText = "";
            ОбновитьИсторию();
        КонецЕсли;
    Исключение
        Сообщить(ОписаниеОшибки());
    КонецПопытки;
КонецПроцедуры

&НаСервере
Функция ОтправитьСообщениеНаСервере(Параметры)
    
    Возврат РеквизитФормыВЗначение("Объект").ОтправитьСообщениеMobileBot(
        Параметры.ExternalChatId,
        Параметры.MessageText, 
        Параметры.BIN,
        Параметры.Author,
        Параметры.Title,
        Параметры.Section,
        Параметры.ChatId);
    
КонецФункции

&НаСервере
Процедура СохранитьСоответствиеНаСервере(ExternalChatId, ChatId, DialogId)
    РеквизитФормыВЗначение("Объект").СохранитьСоответствиеMobileBot(ExternalChatId, ChatId, DialogId);
КонецПроцедуры

&НаКлиенте
Процедура ОбновитьИсторию(Команда = Неопределено)
    Если Не ЗначениеЗаполнено(ChatId) Тогда Возврат; КонецЕсли;
    Попытка
        Сообщения = ПолучитьИсториюНаСервере(ChatId);
        СохранитьИсториюНаСервере(ExternalChatId, Сообщения);
        ЗаполнитьТаблицуИстории(Сообщения);
    Исключение
        Сообщить(ОписаниеОшибки());
        // покажем последний кэш
        ЗаполнитьИсториюИзРегистра();
    КонецПопытки;
КонецПроцедуры

&НаСервере
Функция ПолучитьИсториюНаСервере(ChatId)
    Возврат РеквизитФормыВЗначение("Объект").ПолучитьИсториюMobileBot(ChatId);
КонецФункции

&НаСервере
Процедура СохранитьИсториюНаСервере(ExternalChatId, Сообщения)
    РеквизитФормыВЗначение("Объект").СохранитьИсториюMobileBot(ExternalChatId, Сообщения);
КонецПроцедуры

Код модуля объекта внешней формы

////////////////////////////////////////////////////////////////////////////////
// Интеграция с MobileTelegramBot — МОДУЛЬ ОБЪЕКТА внешней обработки
// Совместимость со старыми релизами платформы: без СоздатьЗапрос/УстановитьМетод
// Используем HTTPЗапрос(Путь, Заголовки) + HTTPСоединение.ОтправитьДляОбработки()
////////////////////////////////////////////////////////////////////////////////

////////////////////////////////////////////////////////////////////////////////
// ВСПОМОГАТЕЛЬНЫЕ: JSON и дата

Функция ToJSON(Данные) Экспорт
    Запись = Новый ЗаписьJSON;
    Запись.УстановитьСтроку();
    ЗаписатьJSON(Запись, Данные);
    Возврат Запись.Закрыть();
КонецФункции

Функция FromJSON(СтрокаJSON) Экспорт
    Чтение = Новый ЧтениеJSON;
    Чтение.УстановитьСтроку(СтрокаJSON);
    Возврат ПрочитатьJSON(Чтение);
КонецФункции

Функция ДатаИзISO8601(Значение) Экспорт
    Если ТипЗнч(Значение) = Тип("Дата") Тогда
        Возврат Значение;
    КонецЕсли;
    Если Не ЗначениеЗаполнено(Значение) Тогда
        Возврат ТекущаяДата();
    КонецЕсли;

    СтрISO = СокрЛП(Строка(Значение));
    Для Каждого Раздел Из Новый Массив("+","-",".") Цикл
        П = СтрНайти(СтрISO, Раздел);
        Если П > 0 Тогда СтрISO = Лев(СтрISO, П-1); КонецЕсли;
    КонецЦикла;
    СтрISO = СтрЗаменить(СтрISO, "T", " ");
    СтрISO = СтрЗаменить(СтрISO, "Z", "");

    Если СтрДлина(СтрISO) < 19 Тогда Возврат ТекущаяДата(); КонецЕсли;

    Попытка
        Возврат Дата(
            Число(Сред(СтрISO,1,4)),
            Число(Сред(СтрISO,6,2)),
            Число(Сред(СтрISO,9,2)),
            Число(Сред(СтрISO,12,2)),
            Число(Сред(СтрISO,15,2)),
            Число(Сред(СтрISO,18,2)));
    Исключение
        Возврат ТекущаяДата();
    КонецПопытки;
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// КОНСТАНТЫ (заполняются в базе)

Функция БазовыйURLMobileBot() Экспорт
    Возврат Константы.СерверПоддержкиMobileBot.Получить(); // напр., https://xxx.ngrok-free.dev
КонецФункции

Функция ТокенИнтеграцииMobileBot() Экспорт
    Возврат Константы.ТокенИнтеграцииMobileBot.Получить();
КонецФункции

Функция APITокенMobileBot() Экспорт
    Возврат Константы.APITокенMobileBot.Получить();
КонецФункции

Функция ДоступныеРазделыMobileBot() Экспорт
    М = Новый Массив;
    М.Добавить(Новый Структура("Код,Наименование", "general", "Общие вопросы"));
    М.Добавить(Новый Структура("Код,Наименование", "finance", "Финансы"));
    М.Добавить(Новый Структура("Код,Наименование", "support", "Техническая поддержка"));
    М.Добавить(Новый Структура("Код,Наименование", "hr",       "HR и кадры"));
    Возврат М;
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// URL-парсер и создание HTTPСоединение с fallback

Функция РазобратьБазовыйURLMobileBot() Экспорт
    URL = СокрЛП(БазовыйURLMobileBot());
    Если ПустаяСтрока(URL) Тогда
        ВызватьИсключение "Не заполнена константа «Сервер поддержки mobile bot».";
    КонецЕсли;

    SSL  = Неопределено;
    Порт = 0;

    Если Лев(НРег(URL),8) = "https://" Тогда
        SSL = Истина; Порт = 443; URL = Сред(URL,9);
    ИначеЕсли Лев(НРег(URL),7) = "http://" Тогда
        SSL = Ложь;   Порт = 80;  URL = Сред(URL,8);
    КонецЕсли;

    ПозСлэш = СтрНайти(URL, "/");
    Если ПозСлэш > 0 Тогда
        ХостСПортом = Лев(URL, ПозСлэш-1);
        БазаПути    = Сред(URL, ПозСлэш); // начинается с /
    Иначе
        ХостСПортом = URL;
        БазаПути    = "";
    КонецЕсли;

    ПозДвтч = СтрНайти(ХостСПортом, ":");
    Если ПозДвтч > 0 Тогда
        Хост = Лев(ХостСПортом, ПозДвтч-1);
        Порт = Число(Сред(ХостСПортом, ПозДвтч+1));
    Иначе
        Хост = ХостСПортом;
    КонецЕсли;
	
	Хост = СокрЛП(Хост);
    Если ПустаяСтрока(Хост) Тогда
        ВызватьИсключение "Не заполнен хост сервера Mobile Bot.";
    КонецЕсли;

    Если SSL = Неопределено Тогда
        SSL = (Порт = 443);
    КонецЕсли;

    Если Порт = 0 Тогда
        Порт = ?(SSL, 443, 80);
    КонецЕсли;

    Если Порт <= 0 Тогда
        ВызватьИсключение "Не удалось определить порт для подключения к серверу Mobile Bot.";
    КонецЕсли;

    Если Не ПустаяСтрока(БазаПути) И Прав(БазаПути,1) = "/" Тогда
        БазаПути = Лев(БазаПути, СтрДлина(БазаПути)-1);
    КонецЕсли;

    Возврат Новый Структура("Хост,Порт,SSL,БазаПути", Хост, Порт, SSL, БазаПути);
КонецФункции

// --- CАМЫЙ СОВМЕСТИМЫЙ способ создать HTTPСоединение ---
Функция СоздатьСоединение_(Парам) Экспорт
    Если Парам.SSL = Неопределено Тогда
        Парам.SSL = Ложь;
    КонецЕсли;

    // В разных релизах платформы доступны разные конструкторы HTTPСоединение,
    // поэтому пробуем их по очереди — от наиболее явного к запасным вариантам.
    Попытка
        Возврат Новый HTTPСоединение(Парам.Хост, Парам.Порт);
    Исключение
        Адрес = Парам.Хост + ":" + Формат(Парам.Порт, "Ч=0");
		Попытка
            Возврат Новый HTTPСоединение(Адрес);
        Исключение
            // запасной вариант (некоторые сборки ожидают 8-й параметр "ИспользоватьSSL")
            Попытка
                Возврат Новый HTTPСоединение(Адрес, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Неопределено, Парам.SSL);
            Исключение
                ВызватьИсключение "В вашей версии платформы не найден подходящий конструктор HTTPСоединение(). " +
                    "Укажите в константе СерверПоддержкиMobileBot адрес вида http://HOST:PORT (без https) " +
                    "или поставьте локальный reverse-proxy (nginx/caddy) на http://127.0.0.1:8081 → https-бэкенд.";
            КонецПопытки;
        КонецПопытки;
    КонецПопытки;
КонецФункции

Функция ВыполнитьPOST(ОтносительныйПуть, Заголовки, ТелоСтрока) Экспорт
    Парам = РазобратьБазовыйURLMobileBot();
    Соединение = СоздатьСоединение_(Парам);

    // гарантируем соответствие и нужные заголовки
    Если ТипЗнч(Заголовки) <> Тип("Соответствие") Тогда
        Заголовки = Новый Соответствие;
    КонецЕсли;
    Заголовки.Вставить("Host", Парам.Хост);      // без СодержитКлюч
    Заголовки.Вставить("Connection", "close");

    Путь = Парам.БазаПути + ОтносительныйПуть;
    Запрос = Новый HTTPЗапрос(Путь, Заголовки);
    Запрос.УстановитьТелоИзСтроки(?(ЗначениеЗаполнено(ТелоСтрока), ТелоСтрока, ""));

    Возврат Соединение.ОтправитьДляОбработки(Запрос);
КонецФункции

Функция ВыполнитьGET(ОтносительныйПуть, Заголовки) Экспорт
    Парам = РазобратьБазовыйURLMobileBot();
    Соединение = СоздатьСоединение_(Парам);

    Если ТипЗнч(Заголовки) <> Тип("Соответствие") Тогда
        Заголовки = Новый Соответствие;
    КонецЕсли;
    Заголовки.Вставить("Host", Парам.Хост);      // без СодержитКлюч
    Заголовки.Вставить("Connection", "close");

    Путь = Парам.БазаПути + ОтносительныйПуть;
    Запрос = Новый HTTPЗапрос(Путь, Заголовки);

    Возврат Соединение.ОтправитьДляОбработки(Запрос);
КонецФункции


////////////////////////////////////////////////////////////////////////////////
// СЕССИЯ ОПЕРАТОРА (кэш в регистре сведений СессииMobileBot)

Функция ПолучитьСессиюMobileBot() Экспорт
    Запрос = Новый Запрос(
        "ВЫБРАТЬ Сессии.Token, Сессии.Истекает
        |ИЗ РегистрСведений.СессииMobileBot КАК Сессии
        |ГДЕ Сессии.Пользователь = &Пользователь");
    Запрос.УстановитьПараметр("Пользователь", Константы.ЛогинОператораMobileBot.Получить());
    Вб = Запрос.Выполнить().Выбрать();
    Если Вб.Следующий() И Вб.Истекает > ТекущаяДата() Тогда
        Возврат Вб.Token;
    КонецЕсли;
    Возврат Неопределено;
КонецФункции

Процедура СохранитьСессиюMobileBot(Token, МинутыЖизни) Экспорт
    Набор = РегистрыСведений.СессииMobileBot.СоздатьНаборЗаписей();
    Набор.Отбор.Пользователь.Установить(Константы.ЛогинОператораMobileBot.Получить());
    Набор.Прочитать();
    Если Набор.Количество() = 0 Тогда
        Зап = Набор.Добавить();
        Зап.Пользователь = Константы.ЛогинОператораMobileBot.Получить();
    Иначе
        Зап = Набор[0];
    КонецЕсли;
    Зап.Token    = Token;
    Зап.Истекает = ТекущаяДата() + МинутыЖизни*60;
    Набор.Записать();
КонецПроцедуры

Функция ПолучитьСессионныйТокенMobileBot() Экспорт
    Токен = ПолучитьСессиюMobileBot();
    Если ЗначениеЗаполнено(Токен) Тогда Возврат Токен; КонецЕсли;

    Логин  = Константы.ЛогинОператораMobileBot.Получить();
    Пароль = Константы.ПарольОператораMobileBot.Получить();
    Если ПустаяСтрока(Логин) ИЛИ ПустаяСтрока(Пароль) Тогда
        ВызватьИсключение "Не заполнены константы логина/пароля оператора MobileBot.";
    КонецЕсли;

    Заголовки = Новый Соответствие;
    Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    Заголовки.Вставить("X-Api-Token", APITокенMobileBot());

    Тело = Новый Структура("identifier,password", Логин, Пароль);
    Ответ = ВыполнитьPOST("/auth/login", Заголовки, ToJSON(Тело));

    Если Ответ.КодСостояния <> 200 Тогда
        ВызватьИсключение СтрШаблон("Ошибка авторизации MobileBot (%1): %2",
            Ответ.КодСостояния, Ответ.ПолучитьТелоКакСтроку());
    КонецЕсли;

    Рез = FromJSON(Ответ.ПолучитьТелоКакСтроку());
    Минуты = ?(Рез.Свойство("expires_in"), Рез.expires_in, 30);
    СохранитьСессиюMobileBot(Рез.token, Минуты);
    Возврат Рез.token;
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// ВАЛИДАЦИЯ

Процедура ПроверитьКлючевыеПоля(ExternalChatId, Текст, БИН) Экспорт
    Если ПустаяСтрока(ExternalChatId) Тогда ВызватьИсключение "Не заполнен external_chat_id"; КонецЕсли;
    Если ПустаяСтрока(СокрЛП(Текст)) Тогда ВызватьИсключение "Текст сообщения не может быть пустым"; КонецЕсли;
    Если ПустаяСтрока(БИН) Тогда ВызватьИсключение "BIN клиента обязателен"; КонецЕсли;
КонецПроцедуры

Функция ПодготовитьТелоMobileBot(ExternalChatId, Текст, БИН,
                                 Автор = Неопределено,
                                 Заголовок = Неопределено,
                                 Раздел = Неопределено,
                                 ЯвныйChatId = Неопределено) Экспорт
    ПроверитьКлючевыеПоля(ExternalChatId, Текст, БИН);
    Д = Новый Структура;
    Д.Вставить("external_chat_id", ExternalChatId);
    Д.Вставить("text", Текст);
    Д.Вставить("bin", БИН);
    Если ЗначениеЗаполнено(Автор)       Тогда Д.Вставить("author", Автор);       КонецЕсли;
    Если ЗначениеЗаполнено(Заголовок)   Тогда Д.Вставить("title", Заголовок);    КонецЕсли;
    Если ЗначениеЗаполнено(Раздел)      Тогда Д.Вставить("section", Раздел);     КонецЕсли;
    Если ЗначениеЗаполнено(ЯвныйChatId) Тогда Д.Вставить("chat_id", ЯвныйChatId); КонецЕсли;
    Возврат ToJSON(Д);
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// API-операции

Функция ОтправитьСообщениеMobileBot(ExternalChatId, Текст, БИН,
    Автор = Неопределено, Заголовок = Неопределено, Раздел = Неопределено, ЯвныйChatId = Неопределено) Экспорт

    Заголовки = Новый Соответствие;
    Заголовки.Вставить("Content-Type", "application/json; charset=utf-8");
    Заголовки.Вставить("X-Integration-Token", ТокенИнтеграцииMobileBot());

    ТелоJSON = ПодготовитьТелоMobileBot(ExternalChatId, Текст, БИН, Автор, Заголовок, Раздел, ЯвныйChatId);

    Попытка
        Ответ = ВыполнитьPOST("/integrations/1c/messages", Заголовки, ТелоJSON);
    Исключение
        ВызватьИсключение "Сетевой сбой при отправке сообщения: " + ОписаниеОшибки();
    КонецПопытки;

    Если Ответ.КодСостояния <> 200 Тогда
        ВызватьИсключение СтрШаблон("Сервер вернул %1: %2", Ответ.КодСостояния, Ответ.ПолучитьТелоКакСтроку());
    КонецЕсли;

    Возврат FromJSON(Ответ.ПолучитьТелоКакСтроку());
КонецФункции

Функция ПолучитьИсториюMobileBot(ChatId) Экспорт
    Если Не ЗначениеЗаполнено(ChatId) Тогда Возврат Новый Массив; КонецЕсли;

    Заголовки = Новый Соответствие;
    Заголовки.Вставить("X-Api-Token", APITокенMobileBot());
    Заголовки.Вставить("X-Session-Token", ПолучитьСессионныйТокенMobileBot());

    Путь = СтрШаблон("/chats/%1/messages", ChatId);
    Ответ = ВыполнитьGET(Путь, Заголовки);

    Если Ответ.КодСостояния <> 200 Тогда
        ВызватьИсключение СтрШаблон("Не удалось получить историю MobileBot (%1): %2",
            Ответ.КодСостояния, Ответ.ПолучитьТелоКакСтроку());
    КонецЕсли;

    Рез = FromJSON(Ответ.ПолучитьТелоКакСтроку());
    Возврат Рез.messages;
КонецФункции

////////////////////////////////////////////////////////////////////////////////
// Регистры: соответствия и история

Процедура СохранитьСоответствиеMobileBot(ExternalChatId, ChatId, DialogId) Экспорт
    Набор = РегистрыСведений.СоответствияMobileBot.СоздатьНаборЗаписей();
    Набор.Отбор.ExternalChatId.Установить(ExternalChatId);
    Набор.Прочитать();
    Если Набор.Количество() = 0 Тогда
        Зап = Набор.Добавить();
        Зап.ExternalChatId = ExternalChatId;
    Иначе
        Зап = Набор[0];
    КонецЕсли;
    Зап.ChatId         = ChatId;
    Зап.DialogId       = DialogId;
    Зап.ДатаОбновления = ТекущаяДата();
    Набор.Записать();
КонецПроцедуры

Функция ПолучитьСоответствиеMobileBot(ExternalChatId) Экспорт
    Запрос = Новый Запрос(
        "ВЫБРАТЬ ПЕРВЫЕ 1
        |    Соответствия.ChatId,
        |    Соответствия.DialogId,
        |    Соответствия.ДатаОбновления
        |ИЗ РегистрСведений.СоответствияMobileBot КАК Соответствия
        |ГДЕ Соответствия.ExternalChatId = &ExternalChatId");
    Запрос.УстановитьПараметр("ExternalChatId", ExternalChatId);
    Вб = Запрос.Выполнить().Выбрать();
    Если Вб.Следующий() Тогда
        Возврат Вб.ПолучитьСтруктуру();
    Иначе
        Возврат Неопределено;
    КонецЕсли;
КонецФункции

Функция ВыгрузитьИсториюMobileBot(ExternalChatId) Экспорт
    Запрос = Новый Запрос(
        "ВЫБРАТЬ
        |    История.MessageId,
        |    История.ДатаСообщения КАК created_at,
        |    История.Направление   КАК direction,
        |    История.Автор         КАК author,
        |    История.Текст         КАК text
        |ИЗ РегистрСведений.ИсторияMobileBot КАК История
        |ГДЕ История.ExternalChatId = &ExternalChatId
        |УПОРЯДОЧИТЬ ПО История.ДатаСообщения");
    Запрос.УстановитьПараметр("ExternalChatId", ExternalChatId);
    Т = Запрос.Выполнить().Выгрузить();

    Рез = Новый Массив;
    Для Каждого Стр Из Т Цикл
        Э = Новый Структура;
        Э.Вставить("message_id", Стр.MessageId);
        Э.Вставить("created_at", Стр.created_at);
        Э.Вставить("direction",  Стр.direction);
        Э.Вставить("author",     Стр.author);
        Э.Вставить("text",       Стр.text);
        Рез.Добавить(Э);
    КонецЦикла;

    Возврат Рез;
КонецФункции

Процедура СохранитьИсториюMobileBot(ExternalChatId, Сообщения) Экспорт
    Если ПустаяСтрока(ExternalChatId) Или Сообщения = Неопределено Или ТипЗнч(Сообщения) <> Тип("Массив") Тогда Возврат; КонецЕсли;

    Набор = РегистрыСведений.ИсторияMobileBot.СоздатьНаборЗаписей();
    БылиНовые = Ложь;

    Для Каждого М Из Сообщения Цикл
        Если ТипЗнч(М) <> Тип("Структура") Или Не М.Свойство("message_id") Тогда Продолжить; КонецЕсли;

        Проверка = Новый Запрос(
            "ВЫБРАТЬ ПЕРВЫЕ 1 История.MessageId
            |ИЗ РегистрСведений.ИсторияMobileBot КАК История
            |ГДЕ История.ExternalChatId = &ExternalChatId
            |  И История.MessageId      = &MessageId");
        Проверка.УстановитьПараметр("ExternalChatId", ExternalChatId);
        Проверка.УстановитьПараметр("MessageId", М.message_id);
        Если Не Проверка.Выполнить().Пустой() Тогда Продолжить; КонецЕсли;

        Зап = Набор.Добавить();
        Зап.ExternalChatId = ExternalChatId;
        Зап.MessageId      = М.message_id;
        Зап.ChatId         = ?(М.Свойство("chat_id"), М.chat_id, Неопределено);
        Зап.Направление    = ?(М.Свойство("direction"), М.direction, "");
        Зап.Автор          = ?(М.Свойство("author"), М.author, "");
        Зап.Текст          = ?(М.Свойство("text"), М.text, "");
        Зап.ДатаСообщения  = ?(М.Свойство("created_at"), ДатаИзISO8601(М.created_at), ТекущаяДата());
        Зап.ДатаЗагрузки   = ТекущаяДата();
        БылиНовые = Истина;
    КонецЦикла;

    Если БылиНовые Тогда Набор.Записать(); КонецЕсли;
КонецПроцедуры



